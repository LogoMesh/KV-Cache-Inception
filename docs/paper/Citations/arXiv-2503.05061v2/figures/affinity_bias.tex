
\begin{figure*}[t!]
    \centering
    \includegraphics[width=\linewidth]{figures/affinity_bias_plot.pdf}
    \caption{Error analysis for the judge models conditioned on the candidate model being graded. All models are included both as candidates and judges. \textit{Itself} (blue) refers to cases in which the judge model graded responses from itself (as a candidate). \textit{Others} (red) refers to cases in which the judge model graded responses from other models.
}
    \label{fig:affinity_bias}
\end{figure*}
